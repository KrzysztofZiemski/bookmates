export const getBook = id => {
  //return...  fetch(url, {})
};

export const addBook = id => {
  //  return fetch(url, {})
};

export const updateBook = id => {
  //  return fetch(url, {})
};

export const removeBook = id => {
  //  return fetch(url, {})
};